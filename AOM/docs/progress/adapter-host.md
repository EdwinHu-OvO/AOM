# Adapter Host 进度

## 当前状态

- 尚未开始实现。

## 近期目标

- 建立 `aom-adapter-host` Rust crate。
- 定义 `TargetManager`、`ProbeManager`、`RawEventBus`、`SnapshotCollector`、`ActionExecutor` 的最小接口。
- 接入 Electron Probe 的第一版 raw event stream。
