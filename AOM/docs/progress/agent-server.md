# Agent Interaction Layer 进度

## 当前状态

- 尚未开始实现。

## 近期目标

- 建立 `aom-agent-server` 和 `aom-mcp-server`。
- 暴露 `aom.snapshot`、`aom.query`、`aom.capabilities`、`aom.invoke`、`aom.verify` 的最小工具集。
- 设计 Agent 可读的 context pack。
