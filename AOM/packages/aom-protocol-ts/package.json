{
  "name": "@aom/protocol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "pnpm run typecheck && pnpm run build && node tests/fixtures.runtime.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
